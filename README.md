# Arduino-ESP32-Provisioning
Solution that allows setting network credentials and user preferences for ESP32s3 at runtime. Applications can be build without knowledge of the network on which it will be deployed. Configuration is done using a smartphone (Android/Apple) and local/Board IP, gateway, Subnet and mDNS name can be set in browser on phone. 
